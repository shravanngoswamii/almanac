name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Format
        run: pnpm format:check

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build the docs site
        run: pnpm --filter www build

      - name: Build the starter, so a fresh site is never broken
        run: pnpm --filter almanac-starter build

      # The starter is deliberately lean, so it cannot prove the optional
      # features still work. The fixture turns all of them on.
      - name: Build the fixture, so every optional feature stays working
        run: pnpm --filter almanac-fixture build
